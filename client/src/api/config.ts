import axios from "axios";

export const API_URL = {
  baseURL: "",
};

// const Axios = axios.create({ baseURL: API_URL.baseURL });

// export const POST_API = async (url: string) => {
//   const data = await Axios.post(url);
// };
